{
  "name": "deero-management-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "dev:backend": "npm run dev:server --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
